package com.projecki.dynamo;

public enum GamePlayType {

    /**
     * A game play type where every player is playing
     * on their own against other players.
     */
    INDIVIDUAL,

    /**
     * A game type where players are grouped into teams
     * and play against other teams.
     */
    TEAM
}
